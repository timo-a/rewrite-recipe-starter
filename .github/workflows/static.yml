# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["docs/antora"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin' # See 'Supported distributions' for available options
          java-version: '21'
      - name: prepare build.gradle #specify the main class that we need for the docs
        run: |
          sed -i '0,/plugins/ s/^plugins {/plugins { id("application")/' build.gradle.kts
          sed -i '0,/}/ s/^}/}\napplication { mainClass = "io.github.timoa.DocMain" }/' build.gradle.kts

      - name: find out pwd
        run: echo "MY_PATH=$(pwd)" >> "$GITHUB_ENV"
      - name: display gradle version info
        run: |
          ./gradlew --help >> /dev/null #downloads and installs gradle, we don't want to print these messages
          echo "## Version Info for Debugging" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`"                        >> $GITHUB_STEP_SUMMARY
          ./gradlew --version                  >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`"                        >> $GITHUB_STEP_SUMMARY

      - name: compile docs
        run: ./gradlew :run --warning-mode all
      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install Antora with the Antora Lunr Extension
        run: npm i antora @antora/lunr-extension asciidoctor-kroki @asciidoctor/tabs
      - name: Generate Site
        run: npx antora src/docs/config/antora-playbook.yml

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'src/docs/config/build/site'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
